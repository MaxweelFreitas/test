import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('PersonalPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PersonalPage(title: 'Personal')));
    //  final titleFinder = find.text('Personal');
    //  expect(titleFinder, findsOneWidget);
  });
}
