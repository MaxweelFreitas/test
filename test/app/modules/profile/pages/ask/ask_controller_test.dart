import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:test_hubo/app/modules/profile/profile_module.dart';

void main() {
  initModule(ProfileModule());
  // AskController ask;
  //
  setUp(() {
    //     ask = ProfileModule.to.get<AskController>();
  });

  group('AskController Test', () {
    //   test("First Test", () {
    //     expect(ask, isInstanceOf<AskController>());
    //   });

    //   test("Set Value", () {
    //     expect(ask.value, equals(0));
    //     ask.increment();
    //     expect(ask.value, equals(1));
    //   });
  });
}
