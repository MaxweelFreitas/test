import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('AskPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AskPage(title: 'Ask')));
    //  final titleFinder = find.text('Ask');
    //  expect(titleFinder, findsOneWidget);
  });
}
